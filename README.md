# paraphrases_visualization
A script in order to create htmls files with paraphrases for sentences in a text

To install dependencies, run the following command:

```yaml
pip install -r paraph.txt
```


